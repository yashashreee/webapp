{
  "variables": {
    "account_file": "{{ env `GOOGLE_APPLICATION_CREDENTIALS` }}",
    "gcp_project_id": "{{env `PROJECT_ID`}}",
    "gcp_region": "{{env `REGION`}}",
    "image_family": "centos-stream-8",
    "source_image": "projects/centos-cloud/global/images/family/centos-stream-8"
  },
  "builders": [
    {
      "type": "googlecompute",
      "project_id": "{{user `gcp_project_id`}}",
      "source_image_family": "{{user `image_family`}}",
      "account_file": "{{ user `account_file` }}",
      "zone": "{{user `gcp_region`}}-b",
      "ssh_username": "csye6225",
      "image_name": "csye6225-custom-image",
      "image_description": "Custom image for CSYE6225",
      "network": "default",
      "service_account_email": "sa-webapp-packer-vm@yash-cloud.iam.gserviceaccount.com"
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "script": "install.sh"
    }
  ]
}