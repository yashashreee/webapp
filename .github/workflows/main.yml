name: Simple Check

on:
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-late

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Install dependencies
      run: npm install
